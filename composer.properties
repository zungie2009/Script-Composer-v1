server.host=127.0.0.1
server.port=8090
storage.mode=FILE
storage.file.directory=./data/projects

# DATABASE mode uses JDBC and requires a compatible driver on the runtime classpath.
# storage.mode=DATABASE
# storage.jdbc.driver=org.h2.Driver
# storage.jdbc.url=jdbc:h2:file:./data/composer
# storage.jdbc.user=sa
# storage.jdbc.password=
